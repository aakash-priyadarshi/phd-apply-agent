# yaml-language-server: $schema=https://railway.com/railway.schema.json

[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m phd_agent.launch"
healthcheckPath = "/_stcore/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
